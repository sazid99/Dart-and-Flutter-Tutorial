নিচে **Dart Map** নিয়ে কিছু **প্র্যাকটিস প্রশ্ন** দেওয়া হল, যা আপনাকে **Map** সম্পর্কিত সমস্ত গুরুত্বপূর্ণ বিষয় অনুশীলন করতে সাহায্য করবে। এই প্রশ্নগুলোতে **Map** এর বিভিন্ন ফিচার, মেথড এবং কাজ করার উপায় থাকবে।  

---

### **১. Map তৈরি এবং ব্যবহার (Creating and Using Maps)**  
1. **প্রশ্ন ১**: একটি `Map` তৈরি করুন, যেখানে `String` কীগুলো এবং `int` মান থাকবে। উদাহরণ: `{name: Alice, age: 25}`।  
2. **প্রশ্ন ২**: একটি `Map` থেকে একটি নির্দিষ্ট কী-এর মান বের করে প্রিন্ট করুন। উদাহরণ: `{name: Alice, age: 25}` থেকে `age` এর মান বের করুন।  
3. **প্রশ্ন ৩**: একটি `Map` তৈরি করুন এবং এতে একটি নতুন কী-মান জোড়া যোগ করুন। উদাহরণ: `{name: Alice}` এ `age: 25` যোগ করুন।  

---

### **২. Map থেকে উপাদান মুছে ফেলা (Removing Elements from Maps)**  
1. **প্রশ্ন ৪**: একটি `Map` থেকে একটি নির্দিষ্ট কী মুছে ফেলুন। উদাহরণ: `{name: Alice, age: 25}` থেকে `age` কী সরিয়ে দিন।  
2. **প্রশ্ন ৫**: একটি `Map` তৈরি করুন এবং `clear()` মেথড ব্যবহার করে সেটি ফাঁকা করুন।  

---

### **৩. Map এর কী এবং মান নিয়ে কাজ (Working with Keys and Values)**  
1. **প্রশ্ন ৬**: একটি `Map` তৈরি করুন এবং সমস্ত কীগুলোর তালিকা বের করুন। উদাহরণ: `{name: Alice, age: 25}` এর কীগুলো বের করুন।  
2. **প্রশ্ন ৭**: একটি `Map` তৈরি করুন এবং সমস্ত মানগুলোর তালিকা বের করুন। উদাহরণ: `{name: Alice, age: 25}` এর মানগুলো বের করুন।  

---

### **৪. Map এর দৈর্ঘ্য এবং চেকিং (Length and Checking)**  
1. **প্রশ্ন ৮**: একটি `Map` তৈরি করুন এবং সেটির দৈর্ঘ্য (কতগুলো কী-মান জোড়া আছে) বের করুন।  
2. **প্রশ্ন ৯**: একটি `Map` এ `containsKey()` মেথড ব্যবহার করে চেক করুন যে নির্দিষ্ট একটি কী আছে কি না।  
3. **প্রশ্ন ১০**: একটি `Map` এ `containsValue()` মেথড ব্যবহার করে চেক করুন যে নির্দিষ্ট একটি মান আছে কি না।  

---

### **৫. Map এর উপর লুপ চালানো (Iterating Over Maps)**  
1. **প্রশ্ন ১১**: একটি `Map` তৈরি করুন এবং `forEach()` মেথড ব্যবহার করে প্রতিটি কী এবং মান প্রিন্ট করুন।  
2. **প্রশ্ন ১২**: একটি `Map` তৈরি করুন এবং `entries` ব্যবহার করে প্রতিটি কী এবং মান আলাদা করে প্রিন্ট করুন।  

---

### **৬. Map এর মান আপডেট করা (Updating Map Values)**  
1. **প্রশ্ন ১৩**: একটি `Map` তৈরি করুন এবং নির্দিষ্ট একটি কী এর মান আপডেট করুন। উদাহরণ: `{name: Alice, age: 25}` এ `age` এর মান ৩০ করুন।  
2. **প্রশ্ন ১৪**: একটি `Map` তৈরি করুন এবং `update()` মেথড ব্যবহার করে একটি কী এর মান আপডেট করুন।  

---

### **৭. Map এর মধ্যে নতুন মান যোগ করা (Adding Multiple Values)**  
1. **প্রশ্ন ১৫**: একটি `Map` তৈরি করুন এবং `addAll()` মেথড ব্যবহার করে অন্য একটি `Map` এর সব কী-মান যোগ করুন।  
2. **প্রশ্ন ১৬**: দুটি `Map` তৈরি করুন এবং সেগুলো একত্রিত করুন। উদাহরণ: `{a: 1}` এবং `{b: 2}` -> `{a: 1, b: 2}`।  

---

### **৮. Map Filter এবং শর্ত প্রয়োগ (Filtering and Conditional Map)**  
1. **প্রশ্ন ১৭**: একটি `Map` তৈরি করুন এবং `removeWhere()` মেথড ব্যবহার করে এমন কী-মান মুছে ফেলুন যেগুলোর মান ১০ এর কম।  
2. **প্রশ্ন ১৮**: একটি `Map` তৈরি করুন এবং ফিল্টার করে নতুন একটি `Map` বের করুন যেখানে মানগুলো ৫ এর বেশি।  

---

### **৯. Map ম্যানিপুলেশন (Manipulating Maps)**  
1. **প্রশ্ন ১৯**: একটি `Map` তৈরি করুন এবং প্রতিটি মানকে ২ দ্বারা গুণ করার জন্য `map()` মেথড ব্যবহার করুন।  
2. **প্রশ্ন ২০**: একটি `Map` তৈরি করুন এবং নির্দিষ্ট শর্ত অনুযায়ী মান পরিবর্তন করুন। উদাহরণ: `{a: 5, b: 15}` -> `{a: 10, b: 30}`।  

---

### **১০. Map এবং অন্যান্য ডেটা স্ট্রাকচার একত্রে ব্যবহার (Combining Maps with Other Structures)**  
1. **প্রশ্ন ২১**: একটি `Map` এবং একটি `List` তৈরি করুন। এরপর `List` এর মানগুলোকে `Map` এর কীগুলোর সাথে যুক্ত করুন।  
2. **প্রশ্ন ২২**: একটি `Map` তৈরি করুন এবং সেটি থেকে সমস্ত মান একটি নতুন `List` এ নিয়ে আসুন।  

---

### **প্র্যাকটিসের সুবিধা**:  
- এই প্রশ্নগুলো সমাধান করলে আপনি **Dart Map** এর প্রায় সব ফিচার এবং মেথড সম্পর্কে ধারণা পাবেন।  
- কী-মান হ্যান্ডলিং, ফিল্টারিং, আপডেটিং, লুপ চালানো, এবং শর্ত প্রয়োগ করা শিখবেন।  
- এগুলো আপনার **Map** সংক্রান্ত দক্ষতা বৃদ্ধিতে সহায়ক হবে।  

**এখনই অনুশীলন শুরু করুন এবং Map সম্পর্কে দক্ষতা অর্জন করুন!** 😊
