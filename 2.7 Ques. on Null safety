নিচে Dart-এর Null Safety নিয়ে ২০টি প্রশ্ন দেওয়া হলো। প্রতিটি প্রশ্নই `?`, `!`, এবং `late` কিওয়ার্ডের ব্যবহার এবং Null Safety-এর ধারণা পরিষ্কারভাবে বুঝতে সাহায্য করবে। 

### ১. `?` অপারেটরের ব্যবহার
1. একটি `String?` টাইপ ভেরিয়েবল ডিক্লেয়ার করুন এবং এটি `null` সেট করুন। এরপর এটি কনসোল-এ প্রিন্ট করুন। 
2. একটি `int?` ভেরিয়েবল তৈরি করে দেখান কিভাবে `null` ভ্যালুর জন্য ডিফল্ট ভ্যালু সেট করা যায়। 

### ২. `!` অপারেটরের ব্যবহার
3. একটি `String?` ভেরিয়েবল তৈরি করে তাতে `null` সেট করুন। এরপর দেখান কিভাবে এটি ব্যবহার করার সময় `!` অপারেটরের কারণে রানটাইমে এরর হতে পারে।
4. `!` ব্যবহার করে একটি `null`-প্রবণ ভেরিয়েবলকে non-nullable টাইপে রূপান্তর করুন।

### ৩. `late` কিওয়ার্ড
5. `late` কিওয়ার্ড ব্যবহার করে একটি ভেরিয়েবল তৈরি করুন এবং সেটির মান অ্যাসাইন করার আগে এটি প্রিন্ট করুন। 
6. একটি `late` ভেরিয়েবল তৈরি করুন এবং দেখান কীভাবে এটি `null` থেকে রক্ষা করতে পারে।

### ৪. Null-aware অপারেটর
7. `??` অপারেটরের সাহায্যে `null` চেক করে ডিফল্ট ভ্যালু সেট করার কোড লিখুন। 
8. `?.` অপারেটর ব্যবহার করে দেখান কীভাবে একটি null-প্রবণ ভেরিয়েবলের মেথড অ্যাক্সেস করা যায়। 

### ৫. Nullable এবং Non-Nullable টাইপ
9. একটি Nullable এবং Non-Nullable টাইপের মধ্যে পার্থক্য দেখিয়ে কোড লিখুন। 
10. `List<int?>` তৈরি করুন যেখানে কিছু আইটেম `null` থাকতে পারে। লুপের মাধ্যমে `null` ভ্যালু বাদ দিয়ে প্রিন্ট করুন।

### ৬. Null Safety এবং ফাংশন
11. একটি ফাংশন তৈরি করুন যা Nullable প্যারামিটার নেয় এবং `null` চেক করে আউটপুট প্রদান করে। 
12. একটি `late` ভেরিয়েবল ব্যবহার করে দেখান কিভাবে Null Safety নিশ্চিত করা যায়। 

### ৭. কনস্ট্রাক্টর এবং Null Safety
13. একটি ক্লাস তৈরি করুন যেখানে কনস্ট্রাক্টর ব্যবহার করে `late` ভেরিয়েবল ইন্সট্যান্সে মান সেট করুন। 
14. একটি ক্লাস তৈরি করুন যেখানে একটি Nullable ভেরিয়েবল ব্যবহৃত হয়। দেখান কিভাবে Null Safety নিশ্চিত করা যায়।

### ৮. Null Safety এবং ডেটা কালেকশন
15. একটি `Map<String, String?>` তৈরি করুন যেখানে কিছু ভ্যালু `null` থাকতে পারে। সমস্ত `null` ভ্যালু বাদ দিয়ে নতুন একটি Map তৈরি করুন। 
16. একটি `List<int?>` থেকে সমস্ত `null` ভ্যালু সরিয়ে একটি Non-Nullable `List` তৈরি করুন।

### ৯. Null Safety এবং Getter/Setter
17. একটি ক্লাস তৈরি করুন যেখানে একটি Nullable ভেরিয়েবলের জন্য Getter এবং Setter ব্যবহার করা হয়। 

### ১০. Null Safety এবং চেইনিং
18. `?.` এবং `??` একত্রে ব্যবহার করে একটি কোড লিখুন যেখানে একটি Nullable ভেরিয়েবল মেথড কল করে, ডিফল্ট ভ্যালু সেট করে। 
19. একটি কোড লিখুন যেখানে একাধিক `?` অপারেটরের চেইন ব্যবহার করা হয়েছে। 

### ১১. Null Safety এবং ডিফল্ট প্যারামিটার
20. একটি ফাংশন তৈরি করুন যা একটি Optional Nullable প্যারামিটার গ্রহণ করে এবং ডিফল্ট ভ্যালু সেট করে।


